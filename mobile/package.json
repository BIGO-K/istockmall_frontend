{
  "name": "mobile",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "aws s3 sync ./dist s3://mmon-v2-m",
    "clear": "aws cloudfront create-invalidation --distribution-id E178A7LQRY3WN6 --paths '/*'",
    "lint": "npx eslint --ext .js,.ts,.vue src"
  }
}
